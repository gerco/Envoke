# IDE files
.idea

# Build output — root-level binaries only
/ee
/ee.exe
/dist/

# Hugo build output and lock files
/docs/public/
/docs/.hugo_build.lock

# Personal config overrides — never commit these
.envoke.local
*.local

# OS
.DS_Store
Thumbs.db
